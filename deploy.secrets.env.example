# Copy to deploy.secrets.env and replace every value. Do not commit that file.
POSTGRES_PASSWORD=replace-with-a-unique-long-password
RIOT_API_KEY=replace-with-a-riot-key
OPENAI_API_KEY=replace-with-an-openai-key
