# Copy to deploy.env. This file contains non-secret deployment parameters.
# DEPLOY_APP_IMAGE must use a versioned tag; never use latest for deployment.
DEPLOY_APP_IMAGE=lol-insight:0.1.0
APP_HOST_PORT=18080
POSTGRES_DB=lol_insight_deploy
POSTGRES_USER=lol_insight_deploy
